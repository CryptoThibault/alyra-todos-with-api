const Account = () => {
  return (
    <div>
      <h2>Account</h2>
      <button id="create">Créer un compte</button>
      <button id="login">Se connecter</button>
    </div>
  );
};

export default Account;